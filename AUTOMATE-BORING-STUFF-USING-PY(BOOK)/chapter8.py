import pyinputplus as pyinp

'''name=pyinp.inputStr('what is ur name?')
print(name)'''

'''
#min-atleast,max-atmost,lessThan-show be < than the least val,timeout-time to run,limit-no of tries for a single inp/errors for single imp(else code will exit())
age=pyinp.inputInt('what is age')
age=pyinp.inputInt('what is age',min=10)
age=pyinp.inputInt('what is ur age',min=1,max=100,lessThan=95)
print(age)
'''

'''
time_limit=pyinp.inputInt('enter text before the timeout can"t be blank',timeout=10,blank=False)
print(time_limit)

x1=pyinp.inputStr(limit=3)
print(x1)

q_7=pyinp.inputStr(limit=3, default='hello')
print(q_7)
'''


#QUESTIONS:-
#1.No.
#2.to avoid writing long code
#3.inputInt()- to take int input , inputFloat()- to take floating val input 
#4.ques4=pyinputplus.inputInt(min=1,max=99,blank=True)
#5.
#6.RetryLimitException after limits
#7.if input is valid(in those 3 tries/via limit) so the input , else the default val
